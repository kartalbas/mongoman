MONGODB_URI=mongodb://admin:password@localhost:27017

# Authentication (optional - set both to enable auth, leave empty to disable)
MONGOMAN_USERNAME=
MONGOMAN_PASSWORD=
# Optional read-only user
MONGOMAN_READONLY_USERNAME=
MONGOMAN_READONLY_PASSWORD=
# Secret for session signing (auto-derived from MONGODB_URI if not set)
AUTH_SECRET=